# Database Configuration
# For local development, install PostgreSQL and use:
# DATABASE_URL=postgresql://username:password@localhost:5432/catchphrase

# For Railway deployment, this will be automatically set by Railway
# when you add the PostgreSQL plugin

# Server Configuration
PORT=3000
